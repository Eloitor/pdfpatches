file=sources/original.pdf
wget $url --output-document="$file"
cksum=$(sha256sum "$file" | cut -d ' ' -f 1)
if [ "$cksum" != "$checksum" ]; then
    echo "Error: Checksum mismatch"
    echo "$cksum"
    exit 1
fi

